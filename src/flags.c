////////////////////////////////////////////////////////////////////////////////
#include "all.h"
////////////////////////////////////////////////////////////////////////////////


static_assert(
    // 64-bit longs are required to fit in all of the 64 flags (A, B, ..., L_).
    sizeof(long) >= 8, "Long integer size is not big enough!"
);
